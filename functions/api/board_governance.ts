import { authorizeBoardRead, authorizeWrite, body, json, requireDb, type Env } from './_lib';
const views = new Set(['summary','meetings','attendance','ballots']);
export const onRequestGet: PagesFunction<Env> = async ({ env, request }) => { const u=new URL(request.url),boardId=(u.searchParams.get('boardId')||'').trim(),view=u.searchParams.get('view')||'summary'; if(!boardId)return json({error:'boardId_required'},{status:400});if(!views.has(view))return json({error:'unknown_view',allowed:[...views]},{status:400});if(!authorizeBoardRead(request,env,boardId))return json({error:'board_access_denied'},{status:403});try{const db=requireDb(env);const q:Record<string,string>={meetings:`SELECT m.*,COALESCE(SUM(CASE WHEN a.attendance_status='present' THEN 1 ELSE 0 END),0) present_count,COUNT(a.id) invited_count FROM meetings m LEFT JOIN meeting_attendance a ON a.meeting_id=m.id WHERE m.board_id=? GROUP BY m.id ORDER BY m.date DESC`,attendance:`SELECT a.*,m.title meeting_title,m.date,p.name member_name,p.role FROM meeting_attendance a JOIN meetings m ON m.id=a.meeting_id JOIN board_members p ON p.id=a.member_id WHERE a.board_id=? ORDER BY m.date DESC,p.name`,ballots:`SELECT b.*,r.number,r.title,m.name member_name,m.role FROM resolution_ballots b JOIN resolutions r ON r.id=b.resolution_id JOIN board_members m ON m.id=b.member_id WHERE b.board_id=? ORDER BY b.cast_at DESC`};if(view!=='summary')return json({boardId,view,data:(await db.prepare(q[view]).bind(boardId).all()).results});const [meetings,upcoming,quorum,ballots]=await Promise.all([db.prepare("SELECT COUNT(*) count FROM meetings WHERE board_id=? AND status NOT IN ('cancelled')").bind(boardId).first(),db.prepare("SELECT COUNT(*) count FROM meetings WHERE board_id=? AND status='planned'").bind(boardId).first(),db.prepare("SELECT COUNT(*) count FROM meeting_attendance WHERE board_id=? AND attendance_status='present'").bind(boardId).first(),db.prepare("SELECT COUNT(*) count FROM resolution_ballots WHERE board_id=?").bind(boardId).first()]);return json({boardId,view,data:{meetings,upcoming,quorum,ballots}})}catch(error){return json({error:'database_unavailable',detail:error instanceof Error?error.message:'unknown'},{status:503})}};
export const onRequestPost: PagesFunction<Env> = async ({env,request})=>{if(!authorizeWrite(request,env))return json({error:'write_not_authorized'},{status:401});try{const v=await body(request),boardId=String(v?.boardId||''),action=String(v?.action||''),db=requireDb(env);if(!boardId||!action)return json({error:'boardId_and_action_required'},{status:400});if(action==='record_attendance'){const attendanceId=String(v?.attendanceId||'');const status=String(v?.attendanceStatus||'present');if(!['invited','present','absent','excused'].includes(status))return json({error:'invalid_attendance_status'},{status:400});const result=await db.prepare('UPDATE meeting_attendance SET attendance_status=?,conflict_flag=?,conflict_note=? WHERE id=? AND board_id=?').bind(status,v?.conflictFlag?1:0,v?.conflictNote||null,attendanceId,boardId).run();if(!result.meta?.changes)return json({error:'attendance_not_found'},{status:404});return json({ok:true,action,attendanceId,status,requiresHumanReview:Boolean(v?.conflictFlag)})}if(action==='cast_ballot'){const resolutionId=String(v?.resolutionId||''),memberId=String(v?.memberId||''),vote=String(v?.vote||'');if(!['for','against','abstain','recused'].includes(vote))return json({error:'invalid_vote'},{status:400});const ballotId=String(v?.ballotId||`${resolutionId}-${memberId}`);await db.prepare('INSERT INTO resolution_ballots (id,board_id,resolution_id,member_id,vote,note) VALUES (?,?,?,?,?,?) ON CONFLICT(resolution_id,member_id) DO UPDATE SET vote=excluded.vote,note=excluded.note,cast_at=datetime(\'now\')').bind(ballotId,boardId,resolutionId,memberId,vote,v?.note||null).run();return json({ok:true,action,ballotId,vote,signature:'not_configured'})}return json({error:'unknown_action',allowed:['record_attendance','cast_ballot']},{status:400})}catch(error){return json({error:'database_unavailable',detail:error instanceof Error?error.message:'unknown'},{status:503})}};
