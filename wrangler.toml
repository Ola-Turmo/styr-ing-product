name = "styr-ing-product"
compatibility_date = "2024-01-01"

# Cloudflare Pages (SSR mode)
# The pages_build_output_dir is inferred from the Cloudflare Pages project settings.
# D1 binding is configured below — set database_id after running:
#   wrangler d1 create styr-ing-db --remote
#
# Cloudflare credentials needed in GitHub Actions secrets:
#   CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID
#
# [[d1_databases]]
# binding = "DB"          # accessed as env.DB in SSR pages
# database_name = "styr-ing-db"
# # database_id = "REPLACE_AFTER_CREATING_DB"  # run: wrangler d1 create styr-ing-db --remote
