name = "styr-ing"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Static Cloudflare Pages deployment target.
# Deploy the built `dist` directory with:
#   wrangler pages deploy dist --project-name styr-ing --branch main
